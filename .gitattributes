# 仓库以 WSL/Linux 为主要使用环境，脚本必须保持 LF，
# 否则在 Windows 上 clone 之后 bash 会因为 \r 报 "bad interpreter"。
* text=auto eol=lf

*.sh   text eol=lf
*.env  text eol=lf
*.yml  text eol=lf
*.md   text eol=lf
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.ps1  text eol=crlf
