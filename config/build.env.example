# KBManager.Build 本地配置示例
#
# 复制成 build.env（或 config/build.env）即可生效，该文件不会提交到 git。
# 优先级：命令行参数 > 环境变量 > 本文件 > 内置默认值。

# KBManager 源码仓库位置。
# 默认会在「本仓库同级目录」和「当前目录」里自动查找 KBManager/，
# 所以只要两个仓库放在一起，这一行可以留空。
# KB_SOURCE="$HOME/src/KBManager"
KB_SOURCE="$KBM_BUILD_ROOT/../KBManager"

# 构建配置：Release | Debug
CONFIG="Release"

# 目标运行时：win-x64 | win-arm64 | linux-x64
RID="win-x64"

# 自包含发布（1 = 目标机无需安装 .NET，体积约 70~90MB）
SELF_CONTAINED="0"

# 产物根目录
# ARTIFACTS="$KBM_BUILD_ROOT/artifacts"

# deploy 落地目录（WSL 路径写法）
# 留空则自动用 C:\Users\<当前用户名>\KBManager-win
# WIN_DEPLOY_DIR="/mnt/d/KBManager-win"

# NuGet 包缓存目录（留空使用默认的 ~/.nuget/packages）
# NUGET_PACKAGES="$HOME/.nuget/packages"

# 找不到 Windows 用户目录时可以手动指定 Windows 用户名
# KB_WINDOWS_USER="YourName"
